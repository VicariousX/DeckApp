{
  "name": "deckbuilder-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --loader ts-node/esm src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "bulk:refresh": "node --loader ts-node/esm -e \"import { ensureBulkData, bulkStatus } from './src/bulkData.ts'; await ensureBulkData({ force: true }); console.log(bulkStatus());\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^22.13.10",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "typescript": "^5.6.3"
  }
}
